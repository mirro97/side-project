# 브롤스타즈 컴패니언 웹앱 — 01. 홈 구현 플랜

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 앱의 첫 화면을 만든다 — 유저 순위 · 클랜 순위 · 진행 중 이벤트 · 내 계정 네 섹션.

**전제:** [플랜 00](2026-08-25-00-foundation.md)이 완료돼 있어야 한다. 여기서 정의한 타입·토큰·공통 컴포넌트를 이름 그대로 참조한다.

**상위 문서:** [설계 2-2](../specs/2026-08-25-brawl-stars-webapp-design.md) · [디자인 시스템](../specs/2026-08-25-design-system.md) · [컨벤션](../specs/2026-08-26-conventions.md)

---

## 설계 결정 — 캐시 경계

상위 설계 문서 2-2 는 "홈을 `/api/home` 하나로 묶고 내 계정은 캐시하지 않는다"고 적었다. **이 둘은 양립할 수 없다.** 사용자별 데이터가 응답에 섞이면 응답 전체가 캐시 불가능해진다.

배포 후 실측한 값이 이 문제를 크게 만든다.

```
  프록시 경유 호출 바닥값   약 500ms   (Vercel iad1 기준, 웜)
```

묶어서 캐시를 포기하면 **모든 방문자가 랭킹·클럽·이벤트까지 매번 500ms를 낸다.** 반대로 나누면 공유 데이터는 10분에 한 번만 원본을 때리고 나머지는 즉시 응답한다.

그래서 경계를 이렇게 다시 긋는다.

```
  공유 (모든 방문자에게 동일)     유저 순위 · 클랜 순위 · 이벤트
    → /api/home 하나로 묶고 서버에서 병렬 호출, 10분 캐시
    → 서버 컴포넌트에서 직접 호출해 SSR 한다. 클라이언트 왕복이 없다

  개인 (대표 계정별로 다름)       내 계정
    → 대표 계정 태그가 로컬스토리지에 있어 서버가 모른다
    → 클라이언트 컴포넌트가 /api/player/[tag] 를 따로 부른다. 캐시 없음
```

두 요청이 병렬로 흐르므로 체감은 느린 쪽 하나(약 500ms)로 수렴한다. 묶었을 때보다 빠르고, 공유 데이터는 캐시가 먹는다.

**세 개를 각각 부르지 않는 이유**는 그대로 유효하다. 순차로 부르면 1.5초가 된다. `/api/home` 안에서 `Promise.allSettled` 로 병렬 호출한다.

---

## 파일 구조

```
  src/app/[locale]/page.tsx              홈 (서버 컴포넌트)
  src/app/api/home/route.ts              공유 3종 묶음, 10분 캐시
  src/app/api/player/[tag]/route.ts      개인, 캐시 없음

  src/lib/bs/api.ts                      엔드포인트별 호출 함수
  src/lib/home.ts                        대표 브롤러 계산 등 홈 전용 로직

  src/components/rank/RankRow.tsx        플레이어/클럽 공용 행 — 랭킹(03)도 쓴다
  src/components/home/SectionCard.tsx    섹션 컨테이너
  src/components/home/EventRow.tsx       홈용 컴팩트 이벤트 행
  src/components/home/MyAccountCard.tsx  내 계정 (클라이언트)
```

`RankRow` 는 홈과 랭킹이 공유한다. **`components/home/` 이 아니라 `components/rank/` 에 만든다** — 플랜 03 이 그대로 가져다 쓴다.

---

# Tasks

### Task 1: 엔드포인트별 호출 함수

**Files:**
- Create: `src/lib/bs/api.ts`
- Test: `src/lib/bs/api.test.ts`

- [ ] **Step 1: 실패하는 테스트 작성**

```ts
// src/lib/bs/api.test.ts
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest'
import { getRankingsPlayersApi, getRankingsClubsApi, getEventsRotationApi } from './api'

function ok(body: unknown) {
  return new Response(JSON.stringify(body), { status: 200 })
}

beforeEach(() => vi.stubEnv('BRAWL_STARS_TOKEN', 't'))
afterEach(() => { vi.unstubAllEnvs(); vi.unstubAllGlobals() })

describe('getRankingsPlayersApi', () => {
  it('국가코드와 limit 을 경로에 넣는다', async () => {
    const spy = vi.fn().mockResolvedValue(ok({ items: [], paging: { cursors: {} } }))
    vi.stubGlobal('fetch', spy)
    await getRankingsPlayersApi('global', 5)
    expect(spy.mock.calls[0][0]).toBe(
      'https://bsproxy.royaleapi.dev/v1/rankings/global/players?limit=5',
    )
  })

  it('커서가 있으면 after 를 붙인다', async () => {
    const spy = vi.fn().mockResolvedValue(ok({ items: [], paging: { cursors: {} } }))
    vi.stubGlobal('fetch', spy)
    await getRankingsPlayersApi('kr', 30, 'eyJwb3MiOjMwfQ')
    expect(spy.mock.calls[0][0]).toContain('&after=eyJwb3MiOjMwfQ')
  })
})

describe('getRankingsClubsApi', () => {
  it('clubs 경로를 쓴다', async () => {
    const spy = vi.fn().mockResolvedValue(ok({ items: [], paging: { cursors: {} } }))
    vi.stubGlobal('fetch', spy)
    await getRankingsClubsApi('global', 5)
    expect(spy.mock.calls[0][0]).toContain('/rankings/global/clubs?limit=5')
  })
})

describe('getEventsRotationApi', () => {
  it('배열을 그대로 준다', async () => {
    vi.stubGlobal('fetch', vi.fn().mockResolvedValue(ok([{ slotId: 1 }])))
    expect(await getEventsRotationApi()).toHaveLength(1)
  })
})
```

- [ ] **Step 2: 실패 확인**

Run: `npm test -- bs/api`
Expected: FAIL — `Failed to resolve import "./api"`

- [ ] **Step 3: 구현**

```ts
// src/lib/bs/api.ts
import { bsFetch, encodeTag } from './client'
import type {
  ClubRankingEntry,
  EventSlot,
  Paged,
  Player,
  RankingEntry,
} from '@/types/api'

/** 랭킹은 국가당 200위가 상한이다. limit 을 아무리 올려도 200 에서 잘린다 */
export function getRankingsPlayersApi(country: string, limit: number, after?: string) {
  const cursor = after ? `&after=${after}` : ''
  return bsFetch<Paged<RankingEntry>>(`/rankings/${country}/players?limit=${limit}${cursor}`)
}

export function getRankingsClubsApi(country: string, limit: number, after?: string) {
  const cursor = after ? `&after=${after}` : ''
  return bsFetch<Paged<ClubRankingEntry>>(`/rankings/${country}/clubs?limit=${limit}${cursor}`)
}

export function getEventsRotationApi() {
  return bsFetch<EventSlot[]>('/events/rotation')
}

export function getPlayerApi(tag: string) {
  return bsFetch<Player>(`/players/${encodeTag(tag)}`)
}

export function getPlayerBattlelogApi(tag: string) {
  return bsFetch<{ items: unknown[] }>(`/players/${encodeTag(tag)}/battlelog`)
}
```

- [ ] **Step 4: 통과 확인**

Run: `npm test -- bs/api`
Expected: PASS (4 tests)

- [ ] **Step 5: 커밋**

```bash
git add -A
git commit -m "브롤스타즈 엔드포인트별 호출 함수 구현"
```

---

### Task 2: 홈 전용 로직

**Files:**
- Create: `src/lib/home.ts`
- Test: `src/lib/home.test.ts`

- [ ] **Step 1: 실패하는 테스트 작성**

```ts
// src/lib/home.test.ts
import { describe, it, expect } from 'vitest'
import { pickMainBrawler, sortByEndingSoon } from './home'
import type { PlayerBrawler } from '@/types/api'
import type { EventSlot } from '@/types/api'

const brawler = (id: number, trophies: number): PlayerBrawler => ({
  id, name: `B${id}`, power: 11, rank: 20, trophies, highestTrophies: trophies,
  gears: [], starPowers: [], gadgets: [],
})

describe('pickMainBrawler', () => {
  it('트로피가 가장 높은 브롤러를 고른다', () => {
    expect(pickMainBrawler([brawler(1, 500), brawler(2, 900), brawler(3, 700)])?.id).toBe(2)
  })
  it('보유 브롤러가 없으면 null 을 준다', () => {
    expect(pickMainBrawler([])).toBeNull()
  })
  it('동점이면 먼저 나온 쪽을 고른다', () => {
    expect(pickMainBrawler([brawler(7, 800), brawler(8, 800)])?.id).toBe(7)
  })
})

const slot = (id: number, end: string): EventSlot => ({
  startTime: '20260824T000000.000Z', endTime: end, slotId: id,
  event: { id, mode: 'brawlBall', modeId: 5, map: 'M' },
})

describe('sortByEndingSoon', () => {
  const now = new Date('2026-08-24T15:00:00Z')

  it('종료가 임박한 순으로 정렬한다', () => {
    const r = sortByEndingSoon(
      [slot(1, '20260824T200000.000Z'), slot(2, '20260824T160000.000Z')],
      now,
    )
    expect(r.map(s => s.slotId)).toEqual([2, 1])
  })

  it('이미 끝난 슬롯은 제외한다', () => {
    const r = sortByEndingSoon(
      [slot(1, '20260824T140000.000Z'), slot(2, '20260824T160000.000Z')],
      now,
    )
    expect(r.map(s => s.slotId)).toEqual([2])
  })

  it('시각을 파싱하지 못한 슬롯도 제외한다', () => {
    const r = sortByEndingSoon([slot(1, 'garbage'), slot(2, '20260824T160000.000Z')], now)
    expect(r.map(s => s.slotId)).toEqual([2])
  })
})
```

- [ ] **Step 2: 실패 확인**

Run: `npm test -- lib/home`
Expected: FAIL

- [ ] **Step 3: 구현**

```ts
// src/lib/home.ts
import { parseBrawlTime } from './bs/parse'
import type { EventSlot, PlayerBrawler } from '@/types/api'

/**
 * 브롤스타즈 API 에 "대표 브롤러" 개념이 없다.
 * 보유 브롤러 중 트로피가 가장 높은 쪽을 대표로 삼는다.
 */
export function pickMainBrawler(brawlers: PlayerBrawler[]): PlayerBrawler | null {
  let best: PlayerBrawler | null = null
  for (const b of brawlers) {
    if (!best || b.trophies > best.trophies) best = b
  }
  return best
}

/**
 * 종료 임박순 정렬. "지금 뭘 해야 하나"에 바로 답이 되는 정렬이다.
 * 파싱 실패나 이미 끝난 슬롯은 제외한다 — API 는 진행 중인 것만 주지만
 * 응답과 렌더 사이에 만료될 수 있다.
 */
export function sortByEndingSoon(slots: EventSlot[], now: Date = new Date()): EventSlot[] {
  return slots
    .map(s => ({ s, end: parseBrawlTime(s.endTime) }))
    .filter((x): x is { s: EventSlot; end: Date } => x.end !== null && x.end > now)
    .sort((a, b) => a.end.getTime() - b.end.getTime())
    .map(x => x.s)
}
```

- [ ] **Step 4: 통과 확인**

Run: `npm test -- lib/home`
Expected: PASS (6 tests)

- [ ] **Step 5: 커밋**

```bash
git add -A
git commit -m "홈 전용 로직 구현"
```

---

### Task 3: 공유 데이터 라우트

**Files:**
- Create: `src/app/api/home/route.ts`

- [ ] **Step 1: 라우트 작성**

```ts
// src/app/api/home/route.ts
import { NextResponse } from 'next/server'
import { getRankingsClubsApi, getEventsRotationApi, getRankingsPlayersApi } from '@/lib/bs/api'
import { sortByEndingSoon } from '@/lib/home'
import { BsError } from '@/lib/bs/errors'
import type { ClubRankingEntry, EventSlot, RankingEntry } from '@/types/api'

/**
 * 모든 방문자에게 동일한 데이터만 담는다. 그래야 캐시가 먹는다.
 * 대표 계정 정보는 /api/player/[tag] 가 따로 내려준다.
 *
 * 프록시 경유 호출이 약 500ms 이므로 순차로 부르면 1.5초가 된다.
 * allSettled 로 병렬 호출하고, 한 조각이 실패해도 나머지는 내려보낸다.
 */
export const revalidate = 600 // 10분

export type HomeSection<T> = { ok: true; data: T } | { ok: false; kind: string }

function toSection<T>(r: PromiseSettledResult<T>): HomeSection<T> {
  if (r.status === 'fulfilled') return { ok: true, data: r.value }
  const kind = r.reason instanceof BsError ? r.reason.kind : 'Unknown'
  console.error('[api/home]', kind, r.reason)
  return { ok: false, kind }
}

export async function GET() {
  const [players, clubs, events] = await Promise.allSettled([
    getRankingsPlayersApi('global', 5),
    getRankingsClubsApi('global', 5),
    getEventsRotationApi(),
  ])

  return NextResponse.json({
    players: toSection<{ items: RankingEntry[] }>(players),
    clubs: toSection<{ items: ClubRankingEntry[] }>(clubs),
    events: toSection<EventSlot[]>(
      events.status === 'fulfilled'
        ? { status: 'fulfilled', value: sortByEndingSoon(events.value).slice(0, 5) }
        : events,
    ),
  })
}
```

- [ ] **Step 2: 로컬에서 확인**

Run:
```bash
curl -s localhost:3000/api/home | python3 -m json.tool | head -30
```

Expected: `players.ok`, `clubs.ok`, `events.ok` 가 모두 `true` 이고 각각 5건

- [ ] **Step 3: 병렬 호출인지 확인**

전체 응답 시간이 개별 호출(약 500ms)의 세 배가 아니라 한 배 남짓이어야 한다.

```bash
curl -s -o /dev/null -w "%{time_total}s\n" localhost:3000/api/home
```

Expected: 1.0초 미만. 1.5초를 넘으면 순차 호출이 된 것이다.

- [ ] **Step 4: 커밋**

```bash
git add -A
git commit -m "홈 공유 데이터 라우트 구현"
```

---

### Task 4: 플레이어 라우트

**Files:**
- Create: `src/app/api/player/[tag]/route.ts`

- [ ] **Step 1: 라우트 작성**

```ts
// src/app/api/player/[tag]/route.ts
import { NextResponse } from 'next/server'
import { getPlayerApi } from '@/lib/bs/api'
import { isValidTag } from '@/lib/bs/client'
import { BsError } from '@/lib/bs/errors'

/** 대표 계정마다 다르므로 캐시하지 않는다 */
export const dynamic = 'force-dynamic'

export async function GET(_req: Request, { params }: { params: Promise<{ tag: string }> }) {
  const { tag } = await params
  const decoded = decodeURIComponent(tag)

  // 잘못된 태그로 외부 API 를 때리지 않는다
  if (!isValidTag(decoded)) {
    return NextResponse.json({ ok: false, kind: 'BadRequest' }, { status: 400 })
  }

  try {
    return NextResponse.json({ ok: true, data: await getPlayerApi(decoded) })
  } catch (e) {
    const kind = e instanceof BsError ? e.kind : 'Unknown'
    const status = e instanceof BsError ? e.status : 500
    // 403 은 키·IP 문제라 운영 이슈다. 사용자에게는 종류만 알린다
    if (kind === 'Forbidden') console.error('[api/player] Forbidden — 키 또는 IP 확인 필요')
    return NextResponse.json({ ok: false, kind }, { status })
  }
}
```

- [ ] **Step 2: 확인**

```bash
curl -s "localhost:3000/api/player/%232V0UL0GQV8" | python3 -c "import json,sys;d=json.load(sys.stdin);print(d['ok'], d.get('data',{}).get('name'))"
curl -s -o /dev/null -w "잘못된 태그: %{http_code}\n" "localhost:3000/api/player/ABCDEF"
```

Expected: 첫 줄에 `True` 와 플레이어 이름, 둘째 줄 `400`

- [ ] **Step 3: 커밋**

```bash
git add -A
git commit -m "플레이어 조회 라우트 구현"
```

---

### Task 5: RankRow 컴포넌트

플레이어와 클럽 두 변형을 하나로 만든다. **플랜 03 랭킹 탭이 이 컴포넌트를 그대로 쓴다.**

**Files:**
- Create: `src/components/rank/RankRow.tsx`
- Test: `src/components/rank/RankRow.test.tsx`

- [ ] **Step 1: 실패하는 테스트 작성**

```tsx
// src/components/rank/RankRow.test.tsx
import { describe, it, expect } from 'vitest'
import { render, screen } from '@testing-library/react'
import { NextIntlClientProvider } from 'next-intl'
import { RankRow } from './RankRow'
import messages from '../../../messages/en.json'

function wrap(ui: React.ReactNode) {
  return render(
    <NextIntlClientProvider locale="en" messages={messages}>{ui}</NextIntlClientProvider>,
  )
}

describe('RankRow', () => {
  it('플레이어 변형은 이름과 클럽을 보여준다', () => {
    wrap(<RankRow kind="player" rank={1} name="VITAL SHARK" trophies={317958}
                  iconUrl="x.png" subtitle="Heaven" />)
    expect(screen.getByText('VITAL SHARK')).toBeInTheDocument()
    expect(screen.getByText('Heaven')).toBeInTheDocument()
    expect(screen.getByText('317,958')).toBeInTheDocument()
  })

  it('내 계정이면 라벨을 붙인다', () => {
    wrap(<RankRow kind="player" rank={1} name="ME" trophies={100} iconUrl="x.png" isMe />)
    expect(screen.getByText(messages.common.myAccount)).toBeInTheDocument()
  })

  it('내 계정이 아니면 라벨이 없다', () => {
    wrap(<RankRow kind="player" rank={2} name="OTHER" trophies={100} iconUrl="x.png" />)
    expect(screen.queryByText(messages.common.myAccount)).toBeNull()
  })

  it('nameColor 가 있으면 이름에 적용한다', () => {
    wrap(<RankRow kind="player" rank={1} name="C" trophies={1} iconUrl="x.png"
                  nameColor="0xffcb5aff" />)
    expect(screen.getByText('C')).toHaveStyle({ color: '#cb5aff' })
  })

  it('클럽 변형은 멤버 수를 보여준다', () => {
    wrap(<RankRow kind="club" rank={1} name="Heaven" trophies={7484545}
                  iconUrl="b.png" subtitle="30/30" />)
    expect(screen.getByText('30/30')).toBeInTheDocument()
  })
})
```

- [ ] **Step 2: 메시지에 `myAccount` 추가**

`messages/en.json` 과 `ko.json` 의 `common` 블록에 각각 넣는다. 플랜 00 Task 5 의 키 일치 테스트가 누락을 잡는다.

```json
"myAccount": "You"
```

```json
"myAccount": "내 계정"
```

- [ ] **Step 3: 실패 확인**

Run: `npm test -- RankRow`
Expected: FAIL

- [ ] **Step 4: 구현**

```tsx
// src/components/rank/RankRow.tsx
'use client'
import { useTranslations } from 'next-intl'
import { RankBadge } from '@/components/display/RankBadge'
import { TrophyValue } from '@/components/display/TrophyValue'
import { argbToHex } from '@/lib/format'

export interface RankRowProps {
  kind: 'player' | 'club'
  rank: number
  name: string
  trophies: number
  iconUrl: string
  /** 플레이어는 클럽명, 클럽은 멤버 수 */
  subtitle?: string
  /** 플레이어만. 0xffcb5aff 형태의 ARGB */
  nameColor?: string
  isMe?: boolean
  onClick?: () => void
}

export function RankRow({
  rank, name, trophies, iconUrl, subtitle, nameColor, isMe, onClick,
}: RankRowProps) {
  const t = useTranslations('common')
  const color = nameColor ? argbToHex(nameColor) : null

  return (
    <div
      onClick={onClick}
      className={`border-border-subtle flex items-center gap-2.5 border-b px-3 py-2.5 last:border-b-0 ${
        onClick ? 'cursor-pointer' : ''
      } ${isMe ? 'bg-brand/15 rounded-card ring-brand ring-1' : ''}`}
    >
      <RankBadge rank={rank} />
      {/* CDN 이미지는 next/image 최적화를 태우지 않는다 */}
      {/* eslint-disable-next-line @next/next/no-img-element */}
      <img src={iconUrl} alt="" width={28} height={28}
           className="bg-bg-elevated rounded-chip h-7 w-7 shrink-0 object-cover" />
      <div className="min-w-0 flex-1">
        <div className="flex items-center gap-1.5">
          <span className="truncate text-[13px] font-semibold"
                style={color ? { color } : undefined}>
            {name}
          </span>
          {isMe && (
            <span className="bg-brand shrink-0 rounded-[4px] px-1.5 py-0.5 text-[9px] font-bold text-white">
              {t('myAccount')}
            </span>
          )}
        </div>
        {subtitle && <div className="text-text-tertiary truncate text-[11px]">{subtitle}</div>}
      </div>
      <TrophyValue value={trophies} />
    </div>
  )
}
```

- [ ] **Step 5: 통과 확인**

Run: `npm test -- RankRow`
Expected: PASS (5 tests)

- [ ] **Step 6: 커밋**

```bash
git add -A
git commit -m "랭킹 행 공용 컴포넌트 구현"
```

---

### Task 6: SectionCard 와 EventRow

**Files:**
- Create: `src/components/home/SectionCard.tsx`, `src/components/home/EventRow.tsx`

- [ ] **Step 1: SectionCard 구현**

```tsx
// src/components/home/SectionCard.tsx
import Link from 'next/link'

export function SectionCard({
  title, moreHref, moreLabel, children,
}: {
  title: string
  moreHref?: string
  moreLabel?: string
  children: React.ReactNode
}) {
  return (
    <section className="mb-6">
      <div className="mb-2.5 flex items-baseline justify-between">
        <h2 className="text-[15px] font-bold tracking-tight">{title}</h2>
        {moreHref && (
          <Link href={moreHref} className="text-text-secondary text-[12px] font-medium">
            {moreLabel} ›
          </Link>
        )}
      </div>
      <div className="border-border-subtle bg-bg-surface rounded-panel overflow-hidden border">
        {children}
      </div>
    </section>
  )
}
```

- [ ] **Step 2: EventRow 구현**

```tsx
// src/components/home/EventRow.tsx
import { CountdownTimer } from '@/components/display/CountdownTimer'

export function EventRow({
  modeName, mapName, iconUrl, end,
}: {
  modeName: string
  mapName: string
  iconUrl: string
  end: Date
}) {
  return (
    <div className="border-border-subtle flex items-center gap-2.5 border-b px-3 py-2.5 last:border-b-0">
      {/* eslint-disable-next-line @next/next/no-img-element */}
      <img src={iconUrl} alt="" width={26} height={26} className="h-[26px] w-[26px] shrink-0" />
      <div className="min-w-0 flex-1">
        <div className="truncate text-[12px] font-semibold">{modeName}</div>
        <div className="text-text-tertiary truncate text-[11px]">{mapName}</div>
      </div>
      <CountdownTimer end={end} />
    </div>
  )
}
```

- [ ] **Step 3: 커밋**

```bash
git add -A
git commit -m "홈 섹션 컨테이너와 이벤트 행 구현"
```

---

### Task 7: 내 계정 카드

대표 계정 태그가 로컬스토리지에 있으므로 클라이언트 컴포넌트다.

**Files:**
- Create: `src/components/home/MyAccountCard.tsx`

- [ ] **Step 1: 메시지 추가**

`messages/en.json` / `ko.json` 에 `home` 블록을 넣는다.

```json
"home": {
  "userRanking": "Top players",
  "clubRanking": "Top clubs",
  "liveEvents": "Live events",
  "myAccount": "My account",
  "setAccountPrompt": "Set your account to see your stats here",
  "mainBrawler": "Top brawler",
  "trophies": "Trophies",
  "highest": "Personal best",
  "level": "Level"
}
```

```json
"home": {
  "userRanking": "유저 순위",
  "clubRanking": "클랜 순위",
  "liveEvents": "진행 중인 이벤트",
  "myAccount": "내 계정",
  "setAccountPrompt": "대표 계정을 설정하면 내 전적이 여기 표시됩니다",
  "mainBrawler": "대표 브롤러",
  "trophies": "트로피",
  "highest": "최고 기록",
  "level": "레벨"
}
```

- [ ] **Step 2: 구현**

```tsx
// src/components/home/MyAccountCard.tsx
'use client'
import { useEffect, useState } from 'react'
import Link from 'next/link'
import { useTranslations } from 'next-intl'
import { useMainAccount } from '@/hooks/useMainAccount'
import { getBrawler } from '@/lib/game-data'
import { pickMainBrawler } from '@/lib/home'
import { formatTrophies } from '@/lib/format'
import { EmptyState } from '@/components/state/EmptyState'
import { ErrorState } from '@/components/state/ErrorState'
import { RowSkeleton } from '@/components/state/Skeletons'
import type { Player } from '@/types/api'
import type { BsErrorKind } from '@/lib/bs/errors'
import type { Locale } from '@/types/game'

export function MyAccountCard({ locale }: { locale: Locale }) {
  const t = useTranslations('home')
  const tc = useTranslations('common')
  const { mainAccountTag } = useMainAccount()
  const [player, setPlayer] = useState<Player | null>(null)
  const [error, setError] = useState<BsErrorKind | null>(null)
  const [loading, setLoading] = useState(false)

  useEffect(() => {
    if (!mainAccountTag) return
    let cancelled = false
    setLoading(true)
    setError(null)
    fetch(`/api/player/${encodeURIComponent(mainAccountTag)}`)
      .then(r => r.json())
      .then((j: { ok: boolean; data?: Player; kind?: BsErrorKind }) => {
        if (cancelled) return
        if (j.ok && j.data) setPlayer(j.data)
        else setError(j.kind ?? 'Unknown')
      })
      .catch(() => { if (!cancelled) setError('Unknown') })
      .finally(() => { if (!cancelled) setLoading(false) })
    return () => { cancelled = true }
  }, [mainAccountTag])

  if (!mainAccountTag) {
    return (
      <EmptyState
        message={t('setAccountPrompt')}
        action={
          <Link href={`/${locale}/profile`}
                className="bg-brand rounded-card px-3 py-1.5 text-[12px] font-semibold text-white">
            {tc('setAccount')}
          </Link>
        }
      />
    )
  }
  if (loading) return <RowSkeleton count={2} />
  if (error) return <ErrorState kind={error} />
  if (!player) return null

  const top = pickMainBrawler(player.brawlers)
  const meta = top ? getBrawler(top.id) : null

  return (
    <div className="border-border-subtle bg-bg-surface rounded-panel flex items-center gap-3 border p-3.5">
      {meta && top && (
        <div className="bg-bg-elevated rounded-card relative h-16 w-16 shrink-0 overflow-hidden">
          {/* eslint-disable-next-line @next/next/no-img-element */}
          <img src={meta.images.portrait} alt="" className="h-full w-full object-cover" />
          <span className="text-trophy absolute inset-x-0 bottom-0 bg-black/70 py-0.5 text-center text-[9px] font-bold">
            {formatTrophies(top.trophies)}
          </span>
        </div>
      )}
      <div className="min-w-0 flex-1">
        <div className="truncate text-[15px] font-bold">{player.name}</div>
        <div className="text-text-tertiary mt-0.5 truncate text-[11px]">
          {player.tag}
          {meta && ` · ${t('mainBrawler')} ${meta.name[locale]}`}
        </div>
        <div className="mt-2 flex gap-1.5">
          <Stat label={t('trophies')} value={formatTrophies(player.trophies)} accent />
          <Stat label={t('highest')} value={formatTrophies(player.highestTrophies)} />
          <Stat label={t('level')} value={String(player.expLevel)} />
        </div>
      </div>
    </div>
  )
}

function Stat({ label, value, accent }: { label: string; value: string; accent?: boolean }) {
  return (
    <div className="bg-bg-elevated rounded-chip flex-1 px-2 py-1.5 text-center">
      <b className={`block text-[13px] font-bold ${accent ? 'text-trophy' : ''}`}>{value}</b>
      <i className="text-text-tertiary mt-px block text-[9px] not-italic">{label}</i>
    </div>
  )
}
```

- [ ] **Step 3: 커밋**

```bash
git add -A
git commit -m "내 계정 카드 구현"
```

---

### Task 8: 홈 페이지 조립

**Files:**
- Modify: `src/app/[locale]/page.tsx`

- [ ] **Step 1: 페이지 작성**

서버 컴포넌트에서 공유 데이터를 직접 가져온다. 클라이언트 왕복이 없다.

```tsx
// src/app/[locale]/page.tsx
import { getTranslations } from 'next-intl/server'
import { getRankingsClubsApi, getEventsRotationApi, getRankingsPlayersApi } from '@/lib/bs/api'
import { sortByEndingSoon } from '@/lib/home'
import { parseBrawlTime } from '@/lib/bs/parse'
import { getMode } from '@/lib/game-data'
import { BsError, type BsErrorKind } from '@/lib/bs/errors'
import { SectionCard } from '@/components/home/SectionCard'
import { EventRow } from '@/components/home/EventRow'
import { MyAccountCard } from '@/components/home/MyAccountCard'
import { RankRow } from '@/components/rank/RankRow'
import { ErrorState } from '@/components/state/ErrorState'
import type { Locale } from '@/types/game'

/** 모든 방문자에게 동일한 데이터라 10분 캐시가 먹는다 */
export const revalidate = 600

function kindOf(e: unknown): BsErrorKind {
  return e instanceof BsError ? e.kind : 'Unknown'
}

export default async function HomePage({ params }: { params: Promise<{ locale: string }> }) {
  const { locale } = await params
  const l = locale as Locale
  const t = await getTranslations('home')
  const tc = await getTranslations('common')

  // 프록시 경유가 약 500ms 다. 순차로 부르면 1.5초가 되므로 반드시 병렬이어야 한다
  const [players, clubs, events] = await Promise.allSettled([
    getRankingsPlayersApi('global', 5),
    getRankingsClubsApi('global', 5),
    getEventsRotationApi(),
  ])

  return (
    <>
      <SectionCard title={t('myAccount')}>
        <MyAccountCard locale={l} />
      </SectionCard>

      <SectionCard title={t('userRanking')} moreHref={`/${l}/ranking`} moreLabel={tc('seeAll')}>
        {players.status === 'fulfilled' ? (
          players.value.items.map(p => (
            <RankRow
              key={p.tag}
              kind="player"
              rank={p.rank}
              name={p.name}
              nameColor={p.nameColor}
              trophies={p.trophies}
              iconUrl={`https://cdn.brawlify.com/profile-icons/regular/${p.icon.id}.png`}
              subtitle={p.club?.name}
            />
          ))
        ) : (
          <ErrorState kind={kindOf(players.reason)} />
        )}
      </SectionCard>

      <SectionCard title={t('clubRanking')} moreHref={`/${l}/ranking`} moreLabel={tc('seeAll')}>
        {clubs.status === 'fulfilled' ? (
          clubs.value.items.map(c => (
            <RankRow
              key={c.tag}
              kind="club"
              rank={c.rank}
              name={c.name}
              trophies={c.trophies}
              iconUrl={`https://cdn.brawlify.com/club-badges/regular/${c.badgeId}.png`}
              subtitle={`${c.memberCount}/30`}
            />
          ))
        ) : (
          <ErrorState kind={kindOf(clubs.reason)} />
        )}
      </SectionCard>

      <SectionCard title={t('liveEvents')} moreHref={`/${l}/events`} moreLabel={tc('seeAll')}>
        {events.status === 'fulfilled' ? (
          sortByEndingSoon(events.value)
            .slice(0, 5)
            .map(e => {
              const mode = getMode(e.event.modeId)
              const end = parseBrawlTime(e.endTime)
              if (!end) return null
              return (
                <EventRow
                  key={e.slotId}
                  modeName={mode?.name[l] ?? e.event.mode}
                  mapName={e.event.map}
                  // 48000000 오프셋이 붙은 imageId 를 써야 한다. modeId 를 그대로 넣으면 404
                  iconUrl={`https://cdn.brawlify.com/game-modes/regular/${mode?.imageId ?? 0}.png`}
                  end={end}
                />
              )
            })
        ) : (
          <ErrorState kind={kindOf(events.reason)} />
        )}
      </SectionCard>
    </>
  )
}
```

- [ ] **Step 2: 전체 검사**

```bash
npx tsc --noEmit && npm test && npm run build
```

Expected: 전부 통과

- [ ] **Step 3: 로컬에서 눈으로 확인**

Run: `npm run dev` 후 `http://localhost:3000/ko`

확인할 것:
- 네 섹션이 모두 렌더된다
- 대표 계정이 없으므로 내 계정 자리에 설정 유도 카드가 나온다
- 1·2·3위에 금·은·동 배지
- 이벤트 남은 시간이 표시되고 2시간 미만은 주황색
- 게임모드 아이콘이 깨지지 않는다 (깨지면 `imageId` 대신 `modeId` 를 쓴 것이다)

- [ ] **Step 4: 응답 시간 확인**

```bash
curl -s -o /dev/null -w "첫 요청: %{time_total}s\n" localhost:3000/ko
curl -s -o /dev/null -w "캐시 후: %{time_total}s\n" localhost:3000/ko
```

Expected: 첫 요청 1초 미만, 두 번째는 훨씬 빠름. 첫 요청이 1.5초를 넘으면 병렬 호출이 아니다.

- [ ] **Step 5: 커밋**

```bash
git add -A
git commit -m "홈 페이지 조립"
```

---

### Task 9: 배포 확인

- [ ] **Step 1: main 에 병합하고 푸시**

```bash
git switch main
```

```bash
git merge feat/home --no-ff -m "홈 구현 병합"
```

```bash
git push origin main
```

훅이 보호 브랜치를 막으므로 브랜치 전환과 커밋을 **한 줄로 묶지 않는다.**

- [ ] **Step 2: 배포된 홈 확인**

```bash
curl -s -o /dev/null -w "%{http_code}  %{time_total}s\n" https://brawl-side.vercel.app/ko
```

Expected: `200`, 1초 미만

- [ ] **Step 3: 헬스체크가 여전히 정상인지**

```bash
curl -s https://brawl-side.vercel.app/api/health | python3 -c "import json,sys;d=json.load(sys.stdin);print(d['ok'], d['region'])"
```

Expected: `True iad1`

---

## 완료 조건

- [ ] `npm test` 통과, `npx tsc --noEmit` 통과, `npm run build` 통과
- [ ] `/ko` 와 `/en` 에서 네 섹션이 모두 렌더된다
- [ ] 홈 첫 요청이 1초 미만이다 (병렬 호출 확인)
- [ ] 게임모드 아이콘이 깨지지 않는다 (`imageId` 사용 확인)
- [ ] 대표 계정이 없을 때 설정 유도 카드가 나온다
- [ ] 한 섹션이 실패해도 나머지 섹션은 정상 렌더된다
- [ ] 배포된 `https://brawl-side.vercel.app/ko` 가 200 이다

## 다음 플랜에 넘기는 것

- **`RankRow`** — 플랜 03 랭킹 탭이 그대로 쓴다. 무한 스크롤과 상세 패널만 추가하면 된다
- **`getRankingsPlayersApi` / `getRankingsClubsApi`** — 플랜 03 이 커서 파라미터를 써서 재사용한다
- **`getEventsRotationApi` / `sortByEndingSoon`** — 플랜 04 이벤트 탭이 그대로 쓴다
- **`/api/player/[tag]`** — 플랜 06 프로필이 그대로 쓴다
